name: "Build node application"
description: "Composite action to setup and build out node app"
runs:
  using: composite
  steps:
    - name: Print step name
      run: echo "Building of node application is starting.."
      shell: cmd
    - name: Install all dependencies
      run: npm install
      shell: cmd